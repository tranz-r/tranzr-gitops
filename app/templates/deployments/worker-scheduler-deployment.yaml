{{- if .Values.deployments.workerScheduler.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deployments.workerScheduler.name }}
  namespace: {{ .Release.Namespace | default .Values.namespaceOverride }}
  labels:
    app.kubernetes.io/name: {{ include "tranzrmoves.name" . }}-worker
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: worker-scheduler
    {{- include "tranzrmoves.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deployments.workerScheduler.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "tranzrmoves.name" . }}-worker
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: worker-scheduler
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app.kubernetes.io/name: {{ include "tranzrmoves.name" . }}-worker
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: worker-scheduler
        helm.sh/chart: {{ include "tranzrmoves.chart" . }}
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "tranzrmoves.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: worker
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: {{ include "tranzrmoves.imageRef" (dict "root" . "key" "movesWorker") | quote }}
          imagePullPolicy: {{ include "tranzrmoves.imagePullPolicy" . }}
          {{- include "tranzrmoves.platformMessagingStartup" (dict "root" . "entrypoint" "TranzrMoves.Worker.dll" "redis" true "rabbitmq" true) | nindent 10 }}
          env:
            - name: Worker__Role
              value: Scheduler
            {{- include "tranzrmoves.observabilityEnv" (dict "root" . "serviceName" "tranzr-moves-worker-scheduler") | nindent 12 }}
            {{- include "tranzrmoves.platformMessagingPasswordEnv" (dict "root" . "redis" true "rabbitmq" true) | nindent 12 }}
            {{- range $key, $val := .Values.deployments.workerScheduler.payLater }}
            - name: PayLater__{{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            {{- range $key, $val := .Values.deployments.workerScheduler.quoteReminders }}
            - name: QuoteReminders__{{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            {{- range $key, $val := .Values.deployments.workerScheduler.e2eDataPurge }}
            {{- if kindIs "slice" $val }}
            {{- range $i, $item := $val }}
            - name: E2eDataPurge__{{ $key }}__{{ $i }}
              value: {{ $item | quote }}
            {{- end }}
            {{- else }}
            - name: E2eDataPurge__{{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            {{- end }}
            {{- range .Values.deployments.workerScheduler.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
            {{- range .Values.deployments.workerScheduler.envFromSecrets }}
            - name: {{ .name }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.externalSecrets.name }}
                  key: {{ .secretKey }}
            {{- end }}
          {{- with .Values.deployments.workerScheduler.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
