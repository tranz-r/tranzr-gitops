{{- if and .Values.deployments.whisperStt.enabled .Values.deployments.whisperStt.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.deployments.whisperStt.name }}-hpa
  namespace: {{ .Release.Namespace | default .Values.namespaceOverride }}
  labels:
    app.kubernetes.io/name: {{ include "tranzrmoves.name" . }}-whisper-stt
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: whisper-stt
    {{- include "tranzrmoves.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.deployments.whisperStt.name }}
  minReplicas: {{ .Values.deployments.whisperStt.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.deployments.whisperStt.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.deployments.whisperStt.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.deployments.whisperStt.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
{{- end }}
